[logging]
mode = "file"

[http]
host = "0.0.0.0"
port = 3000

[paths]
data_dir = "data"
config_file = "config.json"
token_file = "spotify_token.json"
public_dir = "public"
runtime_script = "spotify_matrix.py"

[spotify]
scope = "user-read-playback-state user-read-currently-playing"

[runtime]
python_bin = "python3"
pairing_ttl_s = 600
